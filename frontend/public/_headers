# Security headers for Cloudflare Pages (also mirrored in vercel.json for Vercel).
# CSP notes:
#  - script-src has NO 'unsafe-inline': the built index.html contains only
#    external <script src> tags, so injected inline script is blocked.
#  - style-src needs 'unsafe-inline' because the UI uses React inline styles
#    (style="" attributes) and a couple of injected <style> blocks.
#  - connect-src allows https:/wss: so a custom API/analytics origin never
#    breaks fetch/EventSource; script injection is already blocked by script-src.
#  - img-src allows https: + data:/blob: for proxied files and SSO avatars.
#  - fonts are self-hosted from /fonts, so style-src/font-src no longer need
#    to trust fonts.googleapis.com / fonts.gstatic.com at all.
/*
  Content-Security-Policy: default-src 'self'; base-uri 'self'; object-src 'none'; frame-ancestors 'self'; form-action 'self'; script-src 'self' https://static.cloudflareinsights.com https://va.vercel-scripts.com; style-src 'self' 'unsafe-inline'; font-src 'self' data:; img-src 'self' data: blob: https:; connect-src 'self' https: wss:; frame-src 'self' blob:; worker-src 'self' blob:; manifest-src 'self'; upgrade-insecure-requests
  X-Content-Type-Options: nosniff
  Referrer-Policy: strict-origin-when-cross-origin
  X-Frame-Options: SAMEORIGIN
  Permissions-Policy: camera=(), microphone=(), usb=(), payment=(), geolocation=(self)

# Font files are immutable for a given filename — rename on any font change.
/fonts/*
  Cache-Control: public, max-age=31536000, immutable
